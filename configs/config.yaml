app:
  name: "Refined Chatbot"
  version: "1.0.0"
  debug: true

server:
  host: "0.0.0.0"
  port: 8111

database:
  url: "${DATABASE_URL:postgresql://admin:123456@localhost:5432/mydb}"

auth:
  secret_key: "${SECRET_KEY:change-me-in-production}"
