# version: "3.9"
# name: refined-chatbot-langfuse

# services:
#   langfuse-postgres:
#     image: postgres:17
#     container_name: refined-chatbot-langfuse-postgres
#     restart: unless-stopped
#     environment:
#       POSTGRES_USER: ${LANGFUSE_POSTGRES_USER:-postgres}
#       POSTGRES_PASSWORD: ${LANGFUSE_POSTGRES_PASSWORD:-postgres}
#       POSTGRES_DB: ${LANGFUSE_POSTGRES_DB:-postgres}
#       TZ: UTC
#       PGTZ: UTC
#     ports:
#       - "${LANGFUSE_POSTGRES_PORT:-5433}:5432"
#     volumes:
#       - langfuse_postgres_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U ${LANGFUSE_POSTGRES_USER:-postgres}"]
#       interval: 5s
#       timeout: 5s
#       retries: 10

#   clickhouse:
#     image: docker.io/clickhouse/clickhouse-server
#     container_name: refined-chatbot-clickhouse
#     restart: unless-stopped
#     user: "101:101"
#     environment:
#       CLICKHOUSE_DB: default
#       CLICKHOUSE_USER: ${LANGFUSE_CLICKHOUSE_USER:-clickhouse}
#       CLICKHOUSE_PASSWORD: ${LANGFUSE_CLICKHOUSE_PASSWORD:-clickhouse}
#     ports:
#       - "${LANGFUSE_CLICKHOUSE_HTTP_PORT:-8123}:8123"
#       - "${LANGFUSE_CLICKHOUSE_TCP_PORT:-9000}:9000"
#     volumes:
#       - clickhouse_data:/var/lib/clickhouse
#       - clickhouse_logs:/var/log/clickhouse-server
#     healthcheck:
#       test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:8123/ping || exit 1"]
#       interval: 5s
#       timeout: 5s
#       retries: 10
#       start_period: 1s

#   minio:
#     image: cgr.dev/chainguard/minio
#     container_name: refined-chatbot-minio
#     restart: unless-stopped
#     entrypoint: sh
#     command: -c 'mkdir -p /data/langfuse && minio server --address ":9000" --console-address ":9001" /data'
#     environment:
#       MINIO_ROOT_USER: ${LANGFUSE_MINIO_ROOT_USER:-minio}
#       MINIO_ROOT_PASSWORD: ${LANGFUSE_MINIO_ROOT_PASSWORD:-miniosecret}
#     ports:
#       - "${LANGFUSE_MINIO_API_PORT:-9090}:9000"
#       - "${LANGFUSE_MINIO_CONSOLE_PORT:-9091}:9001"
#     volumes:
#       - minio_data:/data
#     healthcheck:
#       test: ["CMD", "mc", "ready", "local"]
#       interval: 1s
#       timeout: 5s
#       retries: 5
#       start_period: 1s

#   langfuse-redis:
#     image: docker.io/redis:7
#     container_name: refined-chatbot-langfuse-redis
#     restart: unless-stopped
#     command: ["redis-server", "--requirepass", "${LANGFUSE_REDIS_AUTH:-langfuse-redis-secret}", "--maxmemory-policy", "noeviction"]
#     ports:
#       - "${LANGFUSE_REDIS_PORT:-6380}:6379"
#     volumes:
#       - langfuse_redis_data:/data
#     healthcheck:
#       test: ["CMD", "redis-cli", "-a", "${LANGFUSE_REDIS_AUTH:-langfuse-redis-secret}", "ping"]
#       interval: 3s
#       timeout: 10s
#       retries: 10

#   langfuse-worker:
#     image: docker.io/langfuse/langfuse-worker:3
#     container_name: refined-chatbot-langfuse-worker
#     restart: unless-stopped
#     depends_on:
#       langfuse-postgres:
#         condition: service_healthy
#       langfuse-redis:
#         condition: service_healthy
#       clickhouse:
#         condition: service_healthy
#       minio:
#         condition: service_healthy
#     ports:
#       - "${LANGFUSE_WORKER_PORT:-3030}:3030"
#     environment: &langfuse_env
#       NEXTAUTH_URL: ${LANGFUSE_NEXTAUTH_URL:-http://localhost:3000}
#       DATABASE_URL: postgresql://${LANGFUSE_POSTGRES_USER:-postgres}:${LANGFUSE_POSTGRES_PASSWORD:-postgres}@langfuse-postgres:5432/${LANGFUSE_POSTGRES_DB:-postgres}
#       SALT: ${LANGFUSE_SALT}
#       ENCRYPTION_KEY: ${LANGFUSE_ENCRYPTION_KEY}
#       NEXTAUTH_SECRET: ${LANGFUSE_NEXTAUTH_SECRET}
#       TELEMETRY_ENABLED: ${LANGFUSE_TELEMETRY_ENABLED:-false}
#       LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES: ${LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES:-false}
#       CLICKHOUSE_MIGRATION_URL: clickhouse://clickhouse:9000
#       CLICKHOUSE_URL: http://clickhouse:8123
#       CLICKHOUSE_USER: ${LANGFUSE_CLICKHOUSE_USER:-clickhouse}
#       CLICKHOUSE_PASSWORD: ${LANGFUSE_CLICKHOUSE_PASSWORD:-clickhouse}
#       CLICKHOUSE_CLUSTER_ENABLED: "false"
#       LANGFUSE_USE_AZURE_BLOB: "false"
#       LANGFUSE_S3_EVENT_UPLOAD_BUCKET: ${LANGFUSE_S3_BUCKET:-langfuse}
#       LANGFUSE_S3_EVENT_UPLOAD_REGION: ${LANGFUSE_S3_REGION:-auto}
#       LANGFUSE_S3_EVENT_UPLOAD_ACCESS_KEY_ID: ${LANGFUSE_MINIO_ROOT_USER:-minio}
#       LANGFUSE_S3_EVENT_UPLOAD_SECRET_ACCESS_KEY: ${LANGFUSE_MINIO_ROOT_PASSWORD:-miniosecret}
#       LANGFUSE_S3_EVENT_UPLOAD_ENDPOINT: http://minio:9000
#       LANGFUSE_S3_EVENT_UPLOAD_FORCE_PATH_STYLE: "true"
#       LANGFUSE_S3_EVENT_UPLOAD_PREFIX: events/
#       LANGFUSE_S3_MEDIA_UPLOAD_BUCKET: ${LANGFUSE_S3_BUCKET:-langfuse}
#       LANGFUSE_S3_MEDIA_UPLOAD_REGION: ${LANGFUSE_S3_REGION:-auto}
#       LANGFUSE_S3_MEDIA_UPLOAD_ACCESS_KEY_ID: ${LANGFUSE_MINIO_ROOT_USER:-minio}
#       LANGFUSE_S3_MEDIA_UPLOAD_SECRET_ACCESS_KEY: ${LANGFUSE_MINIO_ROOT_PASSWORD:-miniosecret}
#       LANGFUSE_S3_MEDIA_UPLOAD_ENDPOINT: http://minio:9000
#       LANGFUSE_S3_MEDIA_UPLOAD_FORCE_PATH_STYLE: "true"
#       LANGFUSE_S3_MEDIA_UPLOAD_PREFIX: media/
#       REDIS_HOST: langfuse-redis
#       REDIS_PORT: 6379
#       REDIS_AUTH: ${LANGFUSE_REDIS_AUTH:-langfuse-redis-secret}

#   langfuse-web:
#     image: docker.io/langfuse/langfuse:3
#     container_name: refined-chatbot-langfuse
#     restart: unless-stopped
#     depends_on:
#       langfuse-postgres:
#         condition: service_healthy
#       langfuse-redis:
#         condition: service_healthy
#       clickhouse:
#         condition: service_healthy
#       minio:
#         condition: service_healthy
#     ports:
#       - "${LANGFUSE_WEB_PORT:-3000}:3000"
#     environment:
#       <<: *langfuse_env
#       LANGFUSE_INIT_ORG_ID: ${LANGFUSE_INIT_ORG_ID:-refined-chatbot-org}
#       LANGFUSE_INIT_ORG_NAME: ${LANGFUSE_INIT_ORG_NAME:-Refined Chatbot}
#       LANGFUSE_INIT_PROJECT_ID: ${LANGFUSE_INIT_PROJECT_ID:-refined-chatbot}
#       LANGFUSE_INIT_PROJECT_NAME: ${LANGFUSE_INIT_PROJECT_NAME:-Refined Chatbot}
#       LANGFUSE_INIT_PROJECT_PUBLIC_KEY: ${LANGFUSE_INIT_PROJECT_PUBLIC_KEY:-pk-lf-refined-chatbot-local}
#       LANGFUSE_INIT_PROJECT_SECRET_KEY: ${LANGFUSE_INIT_PROJECT_SECRET_KEY:-sk-lf-refined-chatbot-local}
#       LANGFUSE_INIT_USER_EMAIL: ${LANGFUSE_INIT_USER_EMAIL:-admin@local.dev}
#       LANGFUSE_INIT_USER_PASSWORD: ${LANGFUSE_INIT_USER_PASSWORD:-Admin123!}
#       LANGFUSE_INIT_USER_NAME: ${LANGFUSE_INIT_USER_NAME:-Admin}
#     healthcheck:
#       test: ["CMD-SHELL", "wget -qO- http://$${HOSTNAME}:3000/api/public/health || exit 1"]
#       interval: 30s
#       timeout: 10s
#       retries: 5

# volumes:
#   langfuse_postgres_data:
#   clickhouse_data:
#   clickhouse_logs:
#   minio_data:
#   langfuse_redis_data:
